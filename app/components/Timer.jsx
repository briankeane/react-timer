var React = require('react');
var {Link, IndexLink} = require('react-router');

var Timer = () => {
    return (
            <div>
              <h1>Timer</h1>
            </div>
      );
  };

module.exports = Timer;