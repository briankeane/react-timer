var React = require('react');
var {Link, IndexLink} = require('react-router');

var Countdown = () => {
    return (
            <div>
              <h1>Countdown</h1>
            </div>
      );
  };

module.exports = Countdown;